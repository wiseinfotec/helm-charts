{{- if .Values.secret.pooler }}
{{- if not .Values.secret.pooler.secretRef }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "supabase.secret.pooler" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $key, $value := .Values.secret.pooler }}
{{- if $value }}
{{- if eq (typeOf $value) "string" }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
